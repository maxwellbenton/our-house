<%= form_for HouseImage.new do |form| %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :house_image_url %>
  </div>

  <div class="field">
    <%= form.label :tags %>
    <%= form.text_field :tags, id: :house_image_tags %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
